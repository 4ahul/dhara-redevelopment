i am creating re-development automatic , check the csv properly for getting the flow and steps of re-development , i want to create a project of microservices of the services wwe have in csv, the first 2 steps is the scrapping process from thier website, and 3, 4 are the steps for embedding docs to vector db , all these 4 steps is done, now we need to create rest of the services as microservice , there will be one agent orchestration layer that will control another services, we can use others as tool also if possible, the final output  i want from llm is the feasibility report , the agent needs to me very smart so it automatically generates the feasibility report as a end output, we will use googlee maps api for this step - 'Google earth for Analysing Site' , then will use nocaswebsite for getting 'the height we can make it max at '  this side takes input of lat and long. and we will get this from step 5 google api , we can feed that here , then it will give max height we can max it as per long. and lat., then will move forward to 'Premium/Tax Pricing' service (which also internally handles the Ready Reckoner Rate lookups and calculation formula) then the last step is create a full 'Excel sheet for Feasibility Report' for this we can use pyexcel, i have also attached the example feasible report for your idea that how it looks